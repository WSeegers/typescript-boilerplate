{
	"license": "mit",
	"dependencies": {},
	"devDependencies": {
		"rimraf": "^2.6.3",
		"ts-node-dev": "^1.0.0-pre.32",
		"tslint": "^5.14.0",
		"typescript": "^3.3.4000"
	},
	"scripts": {
		"build": "rimraf ./build && tsc",
		"start": "ts-node-dev --respawn --transpileOnly src/index.ts",
		"lint": "tslint -p ."
	}
}
